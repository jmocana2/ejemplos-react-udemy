import React, { PropTypes } from 'react';

const Header = props => {
  return (
    <header>
        <h1>Cotizador de Seguro de auto</h1>
    </header>
  );
};

Header.propTypes = {
  
};

export default Header;